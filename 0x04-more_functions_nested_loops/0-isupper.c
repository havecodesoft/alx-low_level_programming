#include "main.h"
/**
 * _isupper - checking if a letter is uppercase or not
 * @n: num to be checked
 * Return: 1 for uppercse 0 for else
 */
int _isupper(int n)
{
	if (n >= 65 && n <= 90)
	{
	return (1);
	}
	return (0);
}
