#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

char *_strncpy(char *destination, char *source, int num)
{
	return strncpy(destination, source, num);
}
