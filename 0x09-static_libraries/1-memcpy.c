#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

char *_memcpy(char *destination, char *source, unsigned int num)
{
	return memcpy(destination, source, num);
}
