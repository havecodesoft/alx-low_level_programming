#include "main.h"

/**
 * set_string - a function that sets the value of a
 * pointer to a char.
 * @str: input one
 * @h: input two
 * Return: always 0
 */

void set_string(char **str, char *h)
{
	*str = h;
}
